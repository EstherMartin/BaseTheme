import type { Meta, StoryObj } from '@storybook/angular';
import { BaseLinearGradientComponent } from './base-linear-gradient.component';

const meta: Meta<BaseLinearGradientComponent> = {
  title: 'Base-Components/Lineal_Gradient',
  component: BaseLinearGradientComponent,
  tags: ['autodocs'],
};

export default meta;
type Story = StoryObj<BaseLinearGradientComponent>;

export const BaseLinear: Story = {
};
